buildscript {
    repositories {
        mavenCentral()
        maven { url = "https://maven.minecraftforge.net" }
    }
    dependencies {
        classpath "com.anatawa12.forge:ForgeGradle:2.3-1.0.7"
    }
}
apply plugin: "net.minecraftforge.gradle.forge"

//String baseVersion = "1.12.2-0.7.6"

// For test versions, you can use the build number of github actions, or if it is not defined, put local.
//if (System.getenv("GITHUB_RUN_NUMBER") != null) {
//    version = "${baseVersion}-build${System.getenv("GITHUB_RUN_NUMBER")}"
//} else {
//    version = "${baseVersion}-local"
//}

version = "1.12.2-0.7.7"
group = "com.charles445.simpledifficulty"
archivesBaseName = "SimpleDifficulty"

sourceCompatibility = targetCompatibility = '1.8' // Need this here so eclipse task generates correctly.
compileJava {
    sourceCompatibility = targetCompatibility = '1.8'
}

minecraft {
    version = "1.12.2-14.23.5.2847"
    runDir = "run"
    mappings = "stable_39"
    // makeObfSourceJar = false // an Srg named sources jar is made by default. uncomment this to disable.
}

repositories {
    mavenCentral()
    maven {
        name = "CurseForge"
        url "https://www.cursemaven.com"
    }
    maven {
        name = "Fluidlogged API"
        url 'https://jitpack.io'
    }
}

dependencies {
    // Streams
    deobfCompile "curse.maven:streams-229769:3109315"
    deobfCompile "curse.maven:farseek-229708:3093852"

    // Serene Seasons
    compileOnly "curse.maven:sereneseasons-291874:2799213"

    // Dynamic Trees
    compileOnly "curse.maven:DynamicTrees-252818:3812473"

    // Fluidlogged API
    deobfCompile 'com.github.jbredwards:fluidlogged-api:1.12.2-Latest-SNAPSHOT'
}

processResources{
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    from(sourceSets.main.resources.srcDirs) {
        include 'mcmod.info'
                
        expand 'version':project.version, 'mcversion':project.minecraft.version
    }
    
    from(sourceSets.main.resources.srcDirs) {
        exclude 'mcmod.info'
    }
}

task apiJar(type: Jar){
	from(sourceSets.main.allJava){
		include 'com/charles445/simpledifficulty/api/**'
	}
	
	classifier = 'api'
}

artifacts {
	archives apiJar
}

jar {
    manifest 
	{
        attributes "FMLCorePlugin": "com.charles445.simpledifficulty.asm.CoreLoader"
		attributes "FMLCorePluginContainsFMLMod": "com.charles445.simpledifficulty.SimpleDifficulty"
    }
}
